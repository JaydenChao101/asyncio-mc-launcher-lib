version: 2
updates:
  # 檢查Python的依賴項
  - package-ecosystem: "pip"
    directory: "/" # 根目錄，根據需求調整
    schedule:
      interval: "daily" # 每週檢查一次更新

  # 如果有GitHub Actions，可添加以下配置
  - package-ecosystem: "github-actions"
    directory: "/" # 根目錄
    schedule:
      interval: "daily" # 每週檢查一次更新
